apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.name }}
  clusterIP: 10.244.0.500
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432